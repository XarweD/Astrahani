# Astrakhan Landing (Scaffold)

Стартовый макет проекта под Netlify.

## Команды
- `npm i`
- `npm run dev` (локально)
- `npm run build` (сборка в `dist/`)
- `npm run preview`

## Netlify
Publish directory:
- если без сборки: `public`
- если через build: `dist`
